#include "AppInfo.h"

std::string get_app_version() {
	// this can be replaced by a more sophisticated app version detection 
	// algorithm but for now it returns a fixed string of "1.0.0"
	return "1.0.0";
}
